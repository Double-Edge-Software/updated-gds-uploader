import * as React from 'react';
import { MainContainer } from '../../sharedComponents';
import Typography  from '@material-ui/core/Typography';

export interface FileUploadProps {
}

export const  FileUpload  = (props: FileUploadProps) => {
  return (
    <MainContainer>
      <Typography>

      </Typography>
    </MainContainer>
  );
}
